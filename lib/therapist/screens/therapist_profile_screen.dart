// lib/therapist/screens/therapist_profile_screen.dart (stub — full impl in Task 12)
import 'package:flutter/material.dart';

class TherapistProfileScreen extends StatelessWidget {
  const TherapistProfileScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(child: Text('Profile — coming soon')),
      );
}
